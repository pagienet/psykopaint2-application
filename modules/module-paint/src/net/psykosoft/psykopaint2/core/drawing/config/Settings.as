package net.psykosoft.psykopaint2.core.drawing.config
{

	public class Settings
	{
		// Adds the ability to trigger debugging actions via the keyboard in desktop mode.
		// See Settings.as in the core.
		public static const USE_DEBUG_KEYS:Boolean = true;
	}
}
